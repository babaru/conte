<%= simple_form_for(@account, :html => {:class => 'form-horizontal' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">

  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn btn-inverse' %> <input type="reset" value="<%= t("buttons.reset_form")%>" class="btn"> <%= link_to "#{t('labels.back_to')}#{t('model.list', :model => Account.model_name.human)}", accounts_path, :class => 'pull-right' %>
  </div>
<% end %>
